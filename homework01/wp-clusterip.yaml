apiVersion: v1
kind: Service
metadata:
  name: wp-clusterip
spec:
  type: ClusterIP
  ports:
    - name: "http-port"
      protocol: "TCP"
      port: 3306
      targetPort: 3306
  selector:
    app: wp-db-stateful
